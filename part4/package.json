{
  "name": "bloglist",
  "version": "1.0.0",
  "description": "Blog list application",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "mongoose": "^8.1.0"
  }
}
